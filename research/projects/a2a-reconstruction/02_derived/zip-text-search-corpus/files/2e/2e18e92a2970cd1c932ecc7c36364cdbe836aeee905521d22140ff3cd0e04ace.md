# World Open Re-review 04 — Second Verdict

Verdict: `COUNTER`, narrowed after the first repair.

The reviewer independently confirmed PASS for:

- delta-missing repair;
- explicit positive ack body and stable ack confirmation;
- private/malformed readback downgrade without sentinel persistence;
- package-export driver;
- fresh replay of the real live and holdout journals;
- 0600 journal files and zero privacy-scan matches.

Remaining Defeaters:

1. `runExternalAdoptionPollCycle` replayed the journal with caller-supplied
   `viewerRef` before checking the source→target Viewer mapping; the exported
   reconcile function also accepted arbitrary viewers.
2. HTTP 200 with invalid claim JSON was outside the downgrade path, leaving old
   adopted current.
3. A first invalid/private readback persisted its opaque reference in a status
   observation despite never creating a projection.
4. same-event divergence recorded only the new candidate digest instead of an
   ordered pair for accepted and conflicting candidates.

Repairs moved Viewer validation before every replay, made malformed claim
responses durable stale/unknown updates for existing projections, suppressed
reference-bearing status for first invalid evidence, and journaled both
ordered divergence digests. A third independent review was requested after
the full suite reached 56/56.
