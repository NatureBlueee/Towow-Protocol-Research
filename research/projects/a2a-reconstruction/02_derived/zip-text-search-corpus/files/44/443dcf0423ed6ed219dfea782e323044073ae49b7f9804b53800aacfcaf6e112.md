# Attempt 2 resource identity mismatch

Literal rules still failed because the policy used the `/var/...` display path
while macOS resolved the temporary resource under `/private/var/...`.

This was repaired only by canonicalizing the temporary root before compiling
the policy. The paired attempt 3 succeeded on all declared-file probes.
