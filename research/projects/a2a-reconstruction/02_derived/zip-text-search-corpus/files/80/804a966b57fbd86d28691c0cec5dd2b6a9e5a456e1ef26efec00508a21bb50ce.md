# Failure index

- Environment inventory false negatives from zsh special variable `path`.
- Unborn HEAD caused `git rev-parse HEAD` to fail; represented as null.
- First branch command failed to lock `.git/HEAD` despite success-looking text.
- 16/18 imported baseline artifact paths missing.
- Docker daemon and sandbox-exec unavailable under default seatbelt.
- Boundary attempt 1 allowed all private reads.
- Boundary attempt 2 still allowed reads because `/var` did not match
  `/private/var`.
- Raw attempt 3 summary overclassified finite literal-file coverage.
- No independent model/reviewer; no dynamic private world; no commercial value.

Raw experiment failures are preserved inside `experiments/`, especially run
004's `failures/` and attempt directories.
