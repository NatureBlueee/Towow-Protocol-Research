# Rollback

The change is isolated in commit
`7690db92aee263f1918a54c58be16b271175ab4c`.

Preferred rollback is a normal revert of that commit on the research branch:

```bash
git revert 7690db92aee263f1918a54c58be16b271175ab4c
```

Do not reset or clean the user's main worktree.

Rollback consequence: the poller returns to raw JSON append behavior. Raw bytes
may exist while canonical EventLog readers cannot see a valid event, so the new
fresh-reader regression should fail again.
