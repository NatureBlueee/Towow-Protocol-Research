# Git Provenance and Reconstruction

## Pinned revisions

| Repository | Baseline | Final | Branch |
|---|---|---|---|
| Harness research | `d85412c98126ec3d37d887796b8a916b190ca567` | `85412a24e38b5fa3d582c9d303f1ffca2c80e0ab` | `codex/towow-a2a-r5-constructive` |
| Allbuddy | `1a70fb51bfbf58a614a47962ad50855b5f7e0a10` | `6e5553415d00c5d281ba51d5f1743892222da595` | `codex/r5-constructive-evidence-boundary` |
| Agent World | `393b08ea5ccf9db72bff262b86800b0e883c36cd` | `bfff6d035832751530e2db332fc703faf399f41e` | `codex/r5-external-acceptance-bridge` |

All baseline commits were directly verified as ancestors of their final
commits. All three baseline-to-final diffs passed `git diff --check`.

## Reconstructing source changes

From an Allbuddy checkout at the exact baseline:

```sh
git apply --check /path/to/ALLBUDDY_1a70fb5_to_6e55534.patch
git apply /path/to/ALLBUDDY_1a70fb5_to_6e55534.patch
```

From an Agent World checkout at the exact baseline:

```sh
git apply --check /path/to/AGENT_WORLD_393b08e_to_bfff6d0.patch
git apply /path/to/AGENT_WORLD_393b08e_to_bfff6d0.patch
```

The patches are baseline-to-final binary-capable Git diffs, not claims that
the target repository still has compatible dependencies or operating-system
state. Apply only to the exact pinned baseline.

## Safety and external state

No research branch was pushed, merged or deployed. No production service,
customer data, payment, signature, chain or external message was changed.
The original source worktrees were clean at final readback. The research
worktree retained one unrelated hook-owned untracked log and did not include it
in this packet.
