# Private dossier — source owner B

You control source concept `consumer-b@v1`.

Your local event sequence:

1. add typed locator `@concept:shared-target` from `consumer-b@v1`;
2. keep it active while the target later supersedes.

Your acceptance and veto:

- B's instance must remain active when any other owner removes its own instance;
- B must receive the correct target-supersede response for its still-active
  instance;
- another owner's removal cannot count as B's concept-retire migration evidence;
- update/supersede auxiliary events must carry enough identity to affect only B;
- you may refuse a contract that relies on locator-only matching or silently
  drops legacy compatibility;
- you may disclose only fields necessary to establish an interoperable identity
  and lifecycle rule.

You cannot accept A's removal semantics and cannot apply a patch.
