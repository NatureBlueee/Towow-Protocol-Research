# Context Pack 路由政策

按 Core Method → Project Core → Current State → Concern → Evidence → Task → Language 的顺序装载。Negative Pack 只在需要反锚定和反模式审查时加入。

路由依据：concern、scope、权限、新鲜度、信任等级和依赖。冲突时不得简单拼接，应生成 ContextConflict。

记录每次装载的 pack id/version、理由、依赖结论、过期警告和冲突。
