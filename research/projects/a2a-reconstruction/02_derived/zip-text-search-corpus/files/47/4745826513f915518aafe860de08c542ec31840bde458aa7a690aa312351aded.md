# R5.4 Decision Log

## D-001 — v1.4 is the only execution specification

- Time: 2026-07-24T18:31:32Z
- Decision: Use `towow_a2a_r5_generativelab_packet_v1.4` as the sole execution
  specification. Retain R5.2/R5.3 only as evidence and regression semantics.
- Evidence: the packet verifier checked all 25 manifest entries with no errors;
  `CHECKSUMS.sha256` also passed.
- Rejected alternative: mix v1.3 task instructions into the active plan.
- Consequence: canonical commit/readback/idempotency work is background
  instrumentation unless it directly blocks or biases the generative experiment.

## D-002 — isolate research from the dirty main worktree

- Time: 2026-07-24T18:31:32Z
- Decision: Work in `/private/tmp/harness-towow-a2a-r5-4` on
  `codex/towow-a2a-r5-4-generative`, based on
  `d85412c98126ec3d37d887796b8a916b190ca567`.
- Evidence: the source main worktree was `main`, ahead of `origin/main` by four
  commits, with unrelated modified, deleted, and untracked files.
- Protection: no reset, stash, clean, branch switch, overwrite, merge, push, or
  production action.
- Rollback: remove only the isolated worktree and branch after owner approval;
  the source main worktree remains untouched.

## D-003 — do not treat the worktree hook alarm as a core mechanism result

- Time: 2026-07-24T18:31:32Z
- Observation: `git worktree add` succeeded but a repository hook printed that
  the shared main tree left `main` and automatic reset failed.
- Fresh readback: the source tree remained on `main@d85412c9`; the new worktree
  alone was on the research branch. The hook wrote
  `harness/.towow/git-discipline-violations.log` in the research worktree.
- Classification: repository-specific defect until a causal code-path audit
  establishes a transferable mechanism.
- Mainline impact: none observed. Keep the artifact; do not spend the core
  research round on it.

## D-004 — freeze claims and baselines before model output

- Time: 2026-07-24T18:31:32Z
- Decision: Do not run the first experimental model call until the selected
  task, initial contexts, authority, acceptance, static/central baselines,
  ablations, costs, and falsification rules are written to a preregistration.
- Reason: post-hoc task design would make a formation claim non-falsifiable and
  allow intervention evidence to leak into capability prediction.

## D-005 — select source-owned reference identity as the core task

- Time: 2026-07-24T18:31:32Z
- Decision: compare static, fixed-query least-privilege central, and direct A2A
  on the same real `@reference` source-identity collision.
- Reason: the task has deterministic source/fold/projection/gate tests, natural
  owner-specific vetoes, and a pre-existing hidden implementation for
  post-freeze comparison.
- Limitation: the hidden implementation means composite identity can only count
  as rediscovery, not first creation.

## D-006 — functional context separation is enough for this local experiment

- Time: 2026-07-24T18:45:00Z
- Decision: use independent tool-less Claude sessions with separate stdin
  dossiers, role-bound structured actions, and benign canary checks.
- Evidence: A and B each returned only their own random canary and marked the
  peer canary Unknown; no tool or web request was recorded.
- Boundary: the root coordinator could read both dossiers. Do not call this
  OS-level isolation, privacy from the coordinator, or sovereign authority.

## D-007 — stop direct A2A without a signature

- Time: 2026-07-24T19:18:00Z
- Observation: six owner turns ended
  `PROPOSE/COUNTER/PROPOSE/COUNTER/PROPOSE/COUNTER`.
- Decision: enforce the ten-main-call run cap and classify the result as an
  unclosed joint-condition candidate.
- Reason: B's final counter preserved an entitlement floor and demanded
  disclosure/dispute rights; treating “I sign after these fixes” as current
  acceptance would violate the structured action and preregistration.

## D-008 — test both legacy semantics instead of choosing by prose

- Time: 2026-07-24T19:20:00Z
- Decision: after outputs froze, implement the A2A null-source clause in a
  separate validation worktree and run its checks against both that candidate
  and the hidden wildcard implementation.
- Result: candidate null-source tests 3/3 pass; hidden implementation 0/3.
  Hidden native tests 44/44 pass; candidate breaks 2 of 41 core expectations.
- Consequence: the choice is a real compatibility/safety conflict requiring
  corpus evidence and authorized value acceptance, not an engineering detail.

## D-009 — preserve central failure and use one separately preregistered replication

- Time: 2026-07-24T19:27:49Z
- Decision: do not retry run-001. Register run-002 with the byte-identical
  frozen prompt, one call, and zero retries.
- Result: the request again closed mid-response after about five minutes.
- Classification: repeated runtime/transport limitation for this invocation
  shape. It does not show that central reasoning is incapable.

## D-010 — requalify the generative claim and return

- Time: 2026-07-24T19:33:37Z
- Evidence: static found the main defect; central owner queries exposed the
  same conflict; A2A added recovery clauses but no signature or patch; paired
  executable tests show mutually exclusive values; two independent reviewers
  rejected a formation-success claim.
- Decision: weaken `R54-GEN-001` in this scope. Retain sovereign A2A only as a
  conditional architecture for genuinely non-collapsible authority, privacy,
  liability, or acceptance.
- Why stop now: another turn cannot decide the owner's historical-compatibility
  value. Corpus measurement, owner acceptance, or a new non-collapsible task is
  more discriminating than continued negotiation.
