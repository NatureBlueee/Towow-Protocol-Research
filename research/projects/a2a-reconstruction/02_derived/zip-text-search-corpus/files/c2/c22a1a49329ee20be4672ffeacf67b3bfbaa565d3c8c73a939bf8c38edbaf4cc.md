# Attempt 1 boundary bypass

The task and signature path passed, but the coordinator and both subjects could
read resources the policy intended to deny. This attempt is not sovereign
evidence. The raw summary and records remain in the run root's `outputs/`.

The attempt changed the next action: boundary probes became authoritative over
successful task completion.
