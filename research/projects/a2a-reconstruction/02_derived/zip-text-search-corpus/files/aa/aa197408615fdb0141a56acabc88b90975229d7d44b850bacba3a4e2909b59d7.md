# Next research actions

1. Run one owner-authorized, non-production poller task and verify the emitted
   record through a fresh canonical reader after process restart.
2. Pre-register and test a narrow escalation lifecycle change that separates
   `parked_for_redispatch` from `consumed_by_session`; retain current behavior if
   the distinction does not prevent a concrete error.
3. Repeat Capability Claim scoring across a version holdout and a second local
   environment. Bind source, cwd, parent paths, runtime temp root, authority,
   oracle, and recovery before execution.
4. Use the included GLM 5.2 handoff for an ordinary, reversible local
   replication only after the user supplies credentials through their existing
   secret manager. Do not count configuration or connectivity as research
   acceptance.
5. Compare least-privilege central and genuinely independent-environment
   coordination on the same task and oracle. Measure model tokens, latency,
   human attention, disclosure, handoff loss, reconciliation, and recovery.
6. Verify production-current behavior only under separate explicit production
   authorization.

No attack/defense-related work is proposed or required by these actions.
