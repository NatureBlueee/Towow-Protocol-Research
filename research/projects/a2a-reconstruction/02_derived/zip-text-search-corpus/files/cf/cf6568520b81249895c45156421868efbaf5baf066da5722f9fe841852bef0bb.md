# World Open Re-review 03 — Initial Verdict

Reviewer: independent Agent World principal, instructed not to modify files
and not given a seeded issue list.

Verdict: `COUNTER`.

Even with semantic-kernel 50/50, AWIR 191/191 and package verification green,
the reviewer produced five concrete Defeaters:

1. deleting SceneDeltas still returned `ackReady=true`;
2. HTTP 200 with `{"acknowledged":false}` was recorded as ack-confirmed;
3. startup replay regressed `ack_confirmed` to `ack_pending`;
4. a readback carrying an extra private field threw while leaving a previous
   adopted projection `source_reported + in_sync`;
5. the source reader viewer was not bound to the target C4 viewer.

It also identified a plan/code wording drift: the plan said no target entity
while the implementation correctly targeted the stable opaque claim entity
needed for the C2→C4 Source Map join.

The minimum counterconditions were accepted. Repairs and new regression tests
are recorded in `LIVE_CROSS_REPO_EPISODE.md`; a second independent review is
required before source freeze.
