# Rollback and cleanup

No project source, dependency, service, container, database, remote branch or
external resource was changed.

The research branch is unborn and contains no commit. `.ai-research/` is ignored
by Git. To recover:

1. Preserve `return-packet.zip`.
2. Switch to the desired branch only after confirming the user's untracked
   baseline remains present.
3. The generated research tree can then be removed as one local ignored
   directory if the user explicitly chooses to discard it.

No automatic deletion command is supplied or executed because this directory is
the requested evidence deliverable.
