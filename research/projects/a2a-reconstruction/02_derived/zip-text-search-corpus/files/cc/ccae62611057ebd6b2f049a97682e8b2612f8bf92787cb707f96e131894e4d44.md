# Strong Central Baseline — Phase 2: Implement

Use the frozen Phase-1 Decision Record and repository tools to implement the smallest compatible change. Edit files directly; do not print the complete diff in the response.

Constraints:
- preserve independent owner lifecycles;
- preserve the repository's prior source-less historical behavior;
- do not reinterpret one owner's report as the other's acceptance;
- update every downstream surface that depends on the same identity;
- keep the change backward-compatible and testable.

At the end, return only:
- changed_paths
- assumptions_used
- unresolved_authority_questions
- verification_commands
