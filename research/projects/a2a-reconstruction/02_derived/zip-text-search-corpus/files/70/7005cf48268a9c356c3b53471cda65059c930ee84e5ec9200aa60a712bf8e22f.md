# Hypothesis

The run-001 central builder's connection closure was a one-off transport
failure. One separately preregistered call with the byte-identical frozen
prompt will return a usable least-privilege-central synthesis, allowing a fair
comparison with static and direct-A2A conditions.

Falsifier: the single call again ends with a transport/model error or returns no
usable structured patch.
