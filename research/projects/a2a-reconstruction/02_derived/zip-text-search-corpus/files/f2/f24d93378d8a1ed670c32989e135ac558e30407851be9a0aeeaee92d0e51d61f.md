# World Open Re-review 05 — Third Verdict

Verdict: `COUNTER`.

All prior Viewer, malformed claim/readback, first-invalid existence and
dual-digest repairs passed. A new identity Defeater remained:

```text
reference=R, revision=1, eventId=A, state=adopted
reference=R, revision=1, eventId=B, state=revoked
```

Both deliveries could previously enter ordinary C2/C4 and be acked, allowing a
source to rewrite a revision by changing eventId.

The repair makes durable accepted journals the source-revision identity index.
Before any ordinary semantic mutation, every incoming lineage item is checked
against:

```text
(reference, revision) -> canonical {eventId, coarse readback} digest
```

An exact match is replay. Any eventId or digest difference becomes a typed
divergence with both sorted evidence digests, no new source EventRecord, no
source-state advance and no ack. The regression runs through the real poll
seam. Full semantic-kernel tests reached 57/57 before the fourth review.
