# Test and validation results

## Passed

- `make check`: one active `AGENTS.md`, 18 v1.1 packet checksums, JSON parse.
- read-only smoke: success; protected source aggregate unchanged.
- four `run.json` files validate against
  `schemas/experiment_record.schema.json`.
- 59 JSON files and final research YAML parse.
- 12 Claim evidence path sets resolve.
- independent metric recomputation:
  - stdout acceptance 4/9;
  - outer exit acceptance 4/9;
  - minimal contract 9/9;
  - static capability holdout 0/3;
  - boundary attempts allowed/allowed/denied.
- signature recheck:
  - honest report valid;
  - honest verdict valid;
  - tampered report invalid;
  - signed refusal verdict valid.
- no persisted private-key-like file and no common secret pattern.

## Expected non-zero results

- unborn `git rev-parse --verify HEAD`: 128.
- tampered validator/finalizer: 4/5, representing refusal.
- sandbox canary `cat`: 1, representing enforced denial.

## Warning

The raw successful attempt summary overstates the sovereignty label. Final
classification is corrected to `partial_sovereign`; raw evidence is preserved.

## Not tested

- canonical Harness build/test/runtime;
- independent strong-model Agent;
- dynamic-directory hostile boundary;
- human recognition or commercial value;
- production, payment, signing, chain or external effects.
