# Decision Log

## D-0001 — v1.5 是唯一执行规范

- time: 2026-07-25T04:49:14Z
- decision: 仅以 `towow_a2a_r5_constructive_packet_v1.5` 约束本轮执行；v1.4 只保留为否定控制。
- triggering evidence / failure: v1.5 task packet `verify_task_packet.py` 验证 25 个文件通过；R5.4 无目标域采纳、Acceptance 或复用。
- alternatives considered: 延续 R5.4 源码 identity 任务；继续审计 EventLog/readback。
- why this changes research or design: 主线改为寻找能形成现实条件的协调方法；闭合基础设施降为回归工具。
- what is deliberately deferred: EventLog、幂等、字段、生命周期问题，除非阻塞旗舰或制造假闭环。
- rollback / falsifier: 若现实 episode 暴露这些基础设施直接破坏因果判断，再将对应问题提升到主线。

## D-0002 — 保护脏主树并建立隔离研究工位

- time: 2026-07-25T04:49:14Z
- decision: 从已提交基线 `d85412c98126ec3d37d887796b8a916b190ca567` 创建 `/private/tmp/harness-towow-a2a-r5-constructive`，分支 `codex/towow-a2a-r5-constructive`。
- triggering evidence / failure: 主树 `main` ahead 4，存在多项 tracked/untracked 用户改动和大量活跃 worktree。
- alternatives considered: 在主树直接落盘；复用 R5.4 工位。
- why this changes research or design: 避免污染用户修改与旧否定实验，确保本轮 diff 可独立回滚。
- what is deliberately deferred: 不 reset、stash、clean、切换主树分支，不处理现有 dirty 变化。
- rollback / falsifier: 研究分支可整体丢弃；主树 branch/HEAD 已直接读回仍为 `main@d85412c98`。

## D-0003 — 先竞争候选，不把易测 Bug 当旗舰

- time: 2026-07-25T04:49:14Z
- decision: 至少比较 Harness 当前阻塞、跨产品当前阻塞和真实历史阻塞后再冻结旗舰任务。
- triggering evidence / failure: 评分卡要求非折叠性、条件形成、目标域采纳和 Design Delta，而不是仅“真实代码 + 测试清晰”。
- alternatives considered: 直接复用 session continuity 最近修复；构造隐藏答案任务。
- why this changes research or design: 降低答案泄漏和把普通工程修复误判为联合能力的风险。
- what is deliberately deferred: 在候选冻结前不实现产品修复、不提出能力 Claim。
- rollback / falsifier: 若所有候选都能被 least-privilege central 安全折叠，则记录负结果并缩小 A2A 适用域。

## D-0004 — 选择 Allbuddy → Agent World 隐私保留外部 Acceptance 桥

- time: 2026-07-25T05:02:00Z
- decision: 将 FT-R5C-001 冻结为首个旗舰探索任务，但不冻结协调方法或 receipt schema。
- triggering evidence / failure: Allbuddy 实际消费者以硬编码 outcome 自报 `completed`；World 只接受由自己当前 Command 引起的事件，OQ-004/OQ-009 仍 open。
- alternatives considered: Harness 限额迁移；Allbuddy 单仓 context digest；Towow customer runtime。
- why this changes research or design: 该任务同时要求隐私、独立来源权威、双目标域采纳和诚实 Unknown，可直接检验边界形式与升级条件。
- what is deliberately deferred: 生产连接、真实客户数据、owner 商业价值 Recognition；Harness 限额迁移保留为后续外部 holdout。
- rollback / falsifier: 若 least-privilege central 在同等披露和权威边界下同样形成全部结果，则 A2A 结构必要性主张失败，但边界/状态设计知识仍保留。

## D-0005 — 权威先于 schema，反条件先于字段

- time: 2026-07-25T05:05:15Z
- decision: 先冻结 execution、consumer adoption、World presentation 三层权威边界，再让实际实现产生接口；不预先冻结 receipt 字段、固定 TTL、action/scope、payload commitment 或 HMAC。
- triggering evidence / failure: Allbuddy 局部探针证明现有 API 会把 sentinel 复制进 query/artifact，且 `completed` 没有 canonical consumer readback；World 第一轮提出的固定 15m/5m、双 receipt 与签名绑定缺少现存 issuer、policy 和 key authority。
- alternatives considered: 直接采用 World 第一轮 receipt schema；继续只交换文本；让 producer PASS 直接生成 World authoritative projection。
- why this changes research or design: 两个主体通过相互拒绝消除了伪权威与过度披露，形成了可证伪的共同下一动作，而没有把未知基础设施伪装为已存在能力。
- what is deliberately deferred: 具体字段、签名算法、生产身份、真实客户 adoption 与商业价值 Recognition。
- rollback / falsifier: 若实现无法产生 persistent tenant-bound issuer，或同等最小披露的中心方案无需反条件互动即可得到相同边界，则该方法不升级为 Capability Claim。

## D-0006 — 开放复核击穿第一实现，撤回接口冻结

- time: 2026-07-25T05:24:22Z
- decision: 不把 Allbuddy 第一版 file ledger 接口交给 World；将 episode 重开为 Defeater 驱动的条件形成。
- triggering evidence / failure: 独立复核并发复现中 64 个 `prepare` 最终只保留 21 条；同一 one-shot write capability 的 64 次并发提交有 16 次返回成功。实际 ProductShell 路径还丢弃 control capability、没有 World reader grant，且 persistent `adopted` 先于仅存在于 React 内存的 Artifact。
- alternatives considered: 以 39/39、typecheck 和 ESLint 绿灯冻结接口；把并发与 UI 持久化降为后台工程问题。
- why this changes research or design: 这些失败会制造假闭环，直接破坏“interaction 后出现新的被接受路径”的因果主张；它们属于旗舰核心，不得后台化。
- what is deliberately deferred: 生产 IAM、跨区域高可用和真实客户登录；是否为本地 synthetic authority 所必需，等待双方主体明确边界。
- rollback / falsifier: 只有 target consumer 的 canonical persistence、single-use 线性化和 World 可用的最小 reader path 被现实执行证明，才可重新冻结接口。

## D-0007 — Durable state 不等于 durable authority delivery

- time: 2026-07-25T05:46:12Z
- decision: 第二实现仍不冻结；把 recovery、read-after-write 与 cross-principal claim/ack 提升为旗舰核心条件。
- triggering evidence / failure: 第二位独立复核证明 adoption response 丢失可生成不可发现 orphan；ProductShell 信任 mutation response；World E2E 实为同一 Playwright context 取 bearer。
- alternatives considered: 以 64/64、1/63、刷新恢复和浏览器 1 passed 宣告 source condition 已形成。
- why this changes research or design: 联合路径的能力本体包含 authority 如何到达另一个主体并被其持久接手，不能把 delivery 当普通传输细节。
- what is deliberately deferred: 生产 crash durability、fsync、IAM、retention 与跨主机 HA。
- rollback / falsifier: 若幂等 recovery + durable claim/ack 仍无法让独立 World 进程接手并 read-after-write，则停止扩展该本地 source，保留负结果。

## D-0008 — “可恢复”必须覆盖首次响应、用户终止与精确重放

- time: 2026-07-25T06:14:14Z
- decision: 第三实现仍不冻结；将 session 预建立、abort 不采用、orphan lock recovery、精确 operation replay、revision-specific readback 和 lease token 纳入本地 source 最小条件。
- triggering evidence / failure: 第三位不带既有问题清单的复核通过真实 HTTP/UI 和局部故障探针复现：首次 run 响应丢失后无法 recover；abort marker 会在 reload 后自动采用；孤儿锁持续阻塞；旧 revision operation 可用错误输入重放并返回最新状态。
- alternatives considered: 以 46 个局部测试、浏览器 happy path、64-way 并发和 outbox 原子记录冻结接口；把这些问题归为普通可靠性。
- why this changes research or design: 这些反例分别破坏 authority 可达性、消费者真实意图、恢复路径和事件身份，均会让 World 投影一个不应存在或无法追责的事实。
- what is deliberately deferred: 电源故障级 durability、生产 IAM/HA/rotation/retention、多主机锁和真实租户商业价值。
- rollback / falsifier: 修复后若新的开放复核仍发现同类核心反例，停止继续打磨 Allbuddy 源，记录该 Formation 方法未形成可冻结接口。

## D-0009 — Abort 是权威决策，不是浏览器取消

- time: 2026-07-25T06:31:07Z
- decision: 第四实现仍不冻结 combined source；把 abort-after-commit 交回双方主体形成 monotonic cancel/revoke 语义。
- triggering evidence / failure: 第四位开放复核拦截 adoption 响应但允许服务端先提交；用户终止后 UI 删除 marker 并声称没有 Artifact，而独立 World HTTP consumer 仍成功 claim/read/persist/ack adopted@rev1。
- alternatives considered: 把 abort 竞态降为 UI 文案；仅保留 marker 并长期显示 Unknown；忽略已经写入的 outbox。
- why this changes research or design: 用户终止代表 consumer authority 的现实意图变化。若它不进入 canonical state machine，ProductShell 和 World 会同时持有互相矛盾的权威事实。
- what is deliberately deferred: 生产分布式 transaction、真实租户身份、多主机 cancel ordering 与业务价值 Recognition。
- rollback / falsifier: 若串行化 cancel/revoke 后仍能出现 UI “未采用”而 World 最终 adopted，停止该源候选；若只能靠隐藏历史或降低目标修复，同样视为失败。

## D-0010 — 冻结本地 synthetic source，不外推真实 Acceptance

- time: 2026-07-25T06:52:11Z
- decision: 接受第五次开放复核的 bounded PASS，冻结 Allbuddy HTTP source interface 并进入 World 实现；自动采用策略、24h recovery 和单机 authority 写入契约。
- triggering evidence / failure: 独立 HTTP 进程验证 response-loss recovery、lease/token replay、coarse readback、ack 后 revocation lineage 和 service restart；此前 adoption-response 竞态已变为 monotonic canonical revocation。
- alternatives considered: 因生产未知继续无限打磨源端；把 synthetic ProductShell adoption 表述为人类或业务 Acceptance。
- why this changes research or design: 源端条件已经足以让另一个主体独立接手并被现实 falsify，同时边界明确禁止最危险的价值外推。
- what is deliberately deferred: 人类偏好、真实业务采用、24h 后 recovery、真实 tenant、生产 IAM/HA/rotation/power-loss。
- rollback / falsifier: 若独立 World 不能仅凭冻结 HTTP 契约完成 claim/read/persist/ack 与单调 projection，则重新打开 source/consumer boundary，不在 World 侧伪造成功。

## D-0011 — 独立 World 形成新的目标域路径

- time: 2026-07-25T07:30:30Z
- decision: 接受 real local adopted@rev1→revoked@rev2 与 fresh-process replay 作为新的 target-domain technical path，不外推人类/商业 Acceptance。
- triggering evidence / failure: 独立 Allbuddy 与 World 进程实际完成 claim/readback/journal/C2/C4/ack；同一 projection ID 跟随 revision，ack 后 fresh World 在 source 无 redelivery 时重建同一状态。
- alternatives considered: 只以 fake fetch/unit tests 支撑；把 World projection 当作 Allbuddy 自报展示。
- why this changes research or design: 初始不存在的跨系统私域保留路径已成为可执行现实状态，满足 Formation Episode 的最低技术后置条件。
- what is deliberately deferred: human/customer preference、Artifact correctness、production identity/HA、多主机 durability。
- rollback / falsifier: producer-only、wrong bearer、restart、revocation 或 privacy 消融任一出现 false projection 即撤回。

## D-0012 — 绿色实现仍须开放复核，World source freeze 被撤回一次

- time: 2026-07-25T07:40:00Z
- decision: 接受独立 World principal 的 COUNTER；在五项后置条件修复前不冻结 World capability。
- triggering evidence / failure: 50/50 通过仍可复现 delta missing ack-ready、acknowledged=false 误确认、ack phase 倒退、private-invalid 后旧 adopted current、Viewer 越权投影。
- alternatives considered: 以 real happy path 和全量测试宣告完成；把这些问题降为后台可靠性。
- why this changes research or design: 这些反例均能制造 false closure 或跨权威泄漏，直接改变形成主张，证明开放 re-review 不是工程仪式。
- what is deliberately deferred: 不借机重构整个 World persistence 或 canonical schema。
- rollback / falsifier: 修复后仍有同类可复现 false ack/current/viewer path，则 capability 维持 COUNTER。

## D-0013 — 冻结窄 CC-R5C-01 并运行未见负状态 holdout

- time: 2026-07-25T07:37:00Z
- decision: 在 holdout 前冻结单机 synthetic adapter 的前瞻 Claim，用独立 drawing-retrieval cancel-before-adopt 路径检验 `not_adopted@rev1`。
- triggering evidence / failure: formation run 已覆盖 adopted/revoked/offline/recovery，但不能回填为能力预测；需要未知 operation/task-family 的负状态。
- alternatives considered: 用 formation 结果直接升级 Capability Claim；再次运行 customer-analysis happy path。
- why this changes research or design: holdout 命中同一 privacy/replay oracle，支持窄 local claim；同时因仅一个任务族而禁止泛化。
- what is deliberately deferred: protocol/version/environment/time holdout、真实客户与 multi-host。
- rollback / falsifier: holdout 若被投为 completed/adopted、restart ID 漂移或 journal 泄私即失败；本次均未发生。

## D-0014 — A2A 用于形成，中心用于冻结后的执行

- time: 2026-07-25T07:44:00Z
- decision: 将 M-R5C-01 限定为 authority discovery + Defeater/countercondition formation 方法；冻结接口后的运行优先 least-privilege central/deterministic coordinator。
- triggering evidence / failure: static baseline 能一次性提出大部分概念架构；central evaluator 能机械执行最终接口，但不能冒充 consumer 形成 adoption；matched central formation 未运行。
- alternatives considered: 声称 sovereign A2A 在全生命周期胜出；声称 static/central 已证明 A2A 无增量。
- why this changes research or design: 证据只支持 adaptive interaction 在现实 falsification 与过度设计削减上的增量，不支持独有架构发明或长期执行优势。
- what is deliberately deferred: 从同一 pre-state 的 matched central formation 与真实人类价值比较。
- rollback / falsifier: 后续 matched central 若以更低成本形成同等边界，则进一步缩小 A2A 适用域；若真实跨组织 holdout 需要持续主权互动，再扩大。

## D-0015 — 第五次 World 开放复核后接受 scoped PASS 并停止

- time: 2026-07-25T08:02:00Z
- decision: 接受 FT-R5C-001 在 synthetic local scope 的最终 PASS，生成返回包并停止继续同类探针。
- triggering evidence / failure: 五轮 World 实现开放复核依次发现并闭合 delta/ack/private/Viewer/claim/revision/restart Defeaters；最终复核复现冲突 journal 在任何 mutation 前 fail-closed，全量 58/58，live 与 holdout fresh replay 保持。
- alternatives considered: 首次 happy path/50 tests 后停止；在最终 PASS 后继续无限 fuzz；扩大到生产/多主机。
- why this changes research or design: 只有经过未预设问题空间的反复复核，scoped positive claim 才不再依赖浅层绿灯；继续运行在契约不变时已无新增判别知识。
- what is deliberately deferred: matched central formation、真实人类 Recognition、真实外部 source、protocol/version/environment/time holdout、生产 durability。
- rollback / falsifier: source contract、journal schema、authorization mapping 或 scope 任一改变，必须重新打开 claim；真实新 Defeater 也立即撤回 PASS。

## D-0016 — 返回目录必须独立可搬迁，不能依赖原 worktree

- time: 2026-07-25T12:22:29Z
- decision: 将 `R5C-RETURN-001` 重建为自包含目录：承重 episode、发现材料、任务输入、状态与决策、Claim ledger、源码补丁、Schema/依赖/模型 provenance、manifest 和 portable verifier 全部随包搬迁；旧的 `FORMATION_EPISODES.md` 索引改为真实目录。
- triggering evidence / failure: 初版 verifier 只检查 8 个总结文件和 11 个 run 文件存在；返回目录单独移动后，`../../runs` 与 `/private/tmp` 实现指针失效；`GIT_AND_VERIFICATION_FINAL.md` 仍把三个已经提交的分支写成 uncommitted。
- alternatives considered: 仅重跑旧 verifier；要求接收者同时复制整个 Harness research worktree；把 absolute worktree path 当作可运行参考。
- why this changes research or design: 可搬迁性本身是证据可复核条件。若 Claim、Defeater、源码差异或 holdout 在脱离原机器后不能定位，checksum 绿灯不构成返回闭环。
- what is deliberately deferred: 不打包 dependency cache、私域 runtime store、credential、原始 provider envelope 或生产环境；缺失项进入 `PROVENANCE_LIMITATIONS.md`，不伪造。
- rollback / falsifier: 将返回目录复制到新路径后运行 verifier 失败、任一 Claim evidence ref 不解析、manifest 漏文件或 patch 不能在精确 baseline 上 `git apply --check`，则返回包不得标记完成。
