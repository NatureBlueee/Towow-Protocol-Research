# Known failures and retained counterexamples

- The first poller baseline command used an incorrect relative test path and
  exited 4 before collection. It is not counted.
- The preregistered poller regression then failed as predicted: raw bytes
  existed, but the fresh canonical reader found zero records.
- Capability task 03 failed its first attempt because the declared pytest temp
  parent did not exist.
- Capability task 04 failed two attempts because the selected temp root did not
  match Python's authoritative system temp directory.
- The Capability Claim missed its 80% accuracy threshold.
- Current escalation event wording can overstate consumption when the answer is
  only parked for future redispatch.
- The optional GLM handoff has not made a model request and is not independent
  replication evidence.
- The first manifest build failed under the system Python 3.9 because the helper
  imported `datetime.UTC`, which exists only in newer Python versions. The
  portable helper was corrected to use `timezone.utc` and rebuilt from scratch.
