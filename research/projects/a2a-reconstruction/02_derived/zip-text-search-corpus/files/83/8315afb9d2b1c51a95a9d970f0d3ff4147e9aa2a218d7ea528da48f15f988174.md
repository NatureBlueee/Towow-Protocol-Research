# CC-R5C-01 Holdout Result

The prospective claim was frozen in
`CAPABILITY_CLAIM_FROZEN_BEFORE_HOLDOUT.md` before this operation.

## Held-out input

- task family: `drawing-retrieval`
- query class: local fixture lookup
- consumer action: cancel before adoption
- expected source result: `not_adopted@rev1`
- source reference: opaque
- separate World journal and World process identity

The operation and its reference were not used in the formation run.

## Result

- source canonical readback:
  `not_adopted@rev1`, observed `2026-07-25T07:37:31.264Z`;
- first World process:
  - `processed=1`;
  - one AdapterSnapshot, EventRecord, claim entity, WorldProjection and
    SceneDelta;
  - projection ID `aw:world-projection:17CJRXBBKSQ4K13A331YHMCQN0`;
  - EventRecord ID `aw:event-record:4BSMRQ9K10EC0DAW6ZTHW42G63`;
  - current state `not_adopted`, `source_reported`, `in_sync`;
- second fresh World process after source ack:
  - `processed=0`;
  - same projection and EventRecord IDs;
  - same semantic counts and current state.

The journal SHA-256 at the observation cutoff was:

```text
2682b37c17cc76b5886ee9e4a806a967d823e8e646c2957fb6ee876d439aa0c9
```

Searches found none of `DWG-101`, the Artifact ID/title, task ID, consumer
session, World bearer, read capability or lease token.

## Verdict

`PASS` for the frozen narrow claim in the stated single-host synthetic domain.
This is one task-family holdout and does not justify a general Capability
Claim. The result supports only that the formed adapter preserves an
authorized negative consumer report, privacy boundary and restart identity.
