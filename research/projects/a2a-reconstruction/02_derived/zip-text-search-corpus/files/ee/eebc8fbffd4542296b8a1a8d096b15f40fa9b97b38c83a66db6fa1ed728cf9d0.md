# Round 01 — Allbuddy principal

Action: `COUNTER`

> 我拒绝把当前 `WorkspaceRunResult` 直接交给 World 当作见证材料，也拒绝把现有 `completed` 等同于“已被 Allbuddy 产品采用”。

- 当前不可达原因：真实 API 探针证明，`advisor-task` 会忽略伪造的 `enterpriseId`，并把输入 sentinel 原样写入顶层 `query`、Artifact 摘要、输入快照和 `data.query`；`customer-analysis` 收到其他租户标识后仍返回佛山样本的 13 家客户、45 笔订单。参谋任务实际走静态 `ADVISOR_OUTCOMES`，没有接入 tenant-scoped `AdvisorTeam.consult`。前端虽会把完成结果放入当前浏览器的 Artifact 状态，但没有持久化 adoption 真相源，也没有外部只读状态面。35 个测试和 184 条 fixture 校验通过，只证明现有局部契约；未覆盖这条 consumer/tenant/witness 链。
- 我控制的权威：Allbuddy 有权判断任务属于哪个 tenant、参谋结果是否有效、产品消费者是否实际采用，以及哪些状态可向外投影。World 无权替我判业务答案或把传输成功解释为 adoption。
- 绝不跨界：不披露 prompt/query、Artifact 正文或 sections/data、企业对象内容、原始 tenant 标识、跨租户存在性信号；不允许 World 读取完整结果、重算建议或自行把状态升级为成功。
- 我愿意做的最小动作：待本地链路真正以认证会话绑定 tenant、实际 consumer 留下权威 adoption 状态后，仅提供一个不可反查的任务关联标识、粗粒度生命周期状态及时间边界；字段名和 receipt 形状暂不预设。Allbuddy 可用合成 sentinel 本地验证：产品确实消费、错租户 default-deny、最小投影不含 sentinel 或企业正文。
- 希望 World 回答/接受：是否接受只做最小状态投影的独立读取与诚实呈现，并承诺遇到不可读取、来源不权威或状态冲突时报告 `UNKNOWN`，而不是推断业务结论；同时说明它能独立观察的真实读取面是什么。
- 可证伪 Witness：同一合成任务必须同时满足——Allbuddy 权威 consumer 可回读 adoption；World 只能凭不可反查标识独立看到同一粗粒度状态；错误 tenant 得不到任务存在性；任一 World 输出均不含 sentinel、prompt 或企业正文；Allbuddy 标记未采用/失败时，World 不得呈现成功。当前探针已使前两项及隐私项失败，因此本轮不能宣称 formation 成立。

## Local evidence reported by principal

- 35 tests passed.
- 184 fixture checks passed.
- Existing test success did not cover tenant binding, consumer adoption or
  external witness.
- Current API echoes a synthetic sentinel through multiple consumer-visible
  fields, making `WorkspaceRunResult` unsafe as a boundary artifact.
