# Command and Exit-Status Record

## Episode verification recorded before return packaging

| Check | Recorded result |
|---|---|
| Allbuddy `pnpm typecheck` | exit 0 / PASS |
| targeted Allbuddy ESLint | exit 0 / PASS |
| Allbuddy Vitest | exit 0; 8 files, 49 tests |
| Allbuddy ProductShell race/recovery E2E | exit 0; 1 test |
| Agent World semantic-kernel | exit 0; 58 tests |
| Agent World AWIR contracts | exit 0; 191 tests |
| Agent World package verifier | exit 0 / PASS |
| live source/World replay | PASS with stable projection identity |
| held-out `not_adopted@rev1` replay | PASS |

The detailed acceptance boundary is in
[`FORMATION_EPISODES/R5C-EP-001/GIT_AND_VERIFICATION_FINAL.md`](../FORMATION_EPISODES/R5C-EP-001/GIT_AND_VERIFICATION_FINAL.md).

## Fresh packaging readback

On 2026-07-25 the packager directly read all three final `HEAD` values, checked
that each baseline is an ancestor, and ran `git diff --check` for each
baseline-to-final range. Every command exited 0.

The packet directory was then copied to a newly created path outside the
research worktree. Running its own `python3 verify_return_packet.py` there
exited 0 with:

```text
R5C-RETURN-001: OK (files=90, claims=19, local_links=36)
```

Both source patches were checked from detached worktrees at their exact pinned
baselines:

```text
Allbuddy 1a70fb51... + ALLBUDDY_1a70fb5_to_6e55534.patch: git apply --check exit 0
Agent World 393b08ea... + AGENT_WORLD_393b08e_to_bfff6d0.patch: git apply --check exit 0
```

The Allbuddy post-checkout hook printed a shared-tree displacement warning
while adding the detached verification worktree. Direct readback immediately
confirmed the shared tree remained `main@1a70fb51...`; the warning did not
describe actual branch displacement.
