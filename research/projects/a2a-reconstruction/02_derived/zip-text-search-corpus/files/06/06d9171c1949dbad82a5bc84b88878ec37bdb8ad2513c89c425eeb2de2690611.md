# 被拒绝、降级或延期的方向

## 已拒绝为规范内核

- 固定“100 个 Agent”作为通用质量要求；
- 一份 Word 作为唯一真相源；
- 所有 Agent 读取全部上下文；
- 用角色扮演替代 CapabilityProfile；
- 让同一 Agent 自产自证高风险产物；
- 由 UI 原型直接改变领域模型；
- 把对外营销文本当作产品定义；
- 把方法质量归结为模型是否聪明。

## 降级为参考实现或可选策略

- 固定五个一级导航；
- 全站深色 Mission Control 风格；
- 常驻右侧 AI 助手；
- 特定组件库和工程栈；
- 多 Agent 的具体编排拓扑；
- OpenTelemetry 的具体集成方式。

## 仍待研究

- Registry 最小可维护形式；
- Capability 资格如何根据历史表现动态更新；
- 大规模并行探索的去相关与停止条件；
- Validator 自身如何验证和防止规则过拟合；
- 方法治理是公司内部、开源项目还是基金会形态；
- PRSE 与召集台未来是否共享一部分协议对象。
