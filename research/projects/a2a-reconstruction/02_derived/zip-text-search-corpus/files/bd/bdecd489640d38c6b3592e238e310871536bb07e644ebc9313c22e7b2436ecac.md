# Model, Agent and Environment Facts

## Environment

- episode date: `2026-07-25`
- host snapshot: macOS 26.6 arm64
- Python: 3.9.6
- host Node recorded at discovery: 26.5.0
- Agent World episode runtime: Node 24.18.0
- Git: 2.50.1
- Codex CLI: 0.145.0
- Claude Code: 2.1.220

The original discovery snapshot is preserved at
[`DISCOVERY/R5C-DISCOVERY-001/ENVIRONMENT.md`](../DISCOVERY/R5C-DISCOVERY-001/ENVIRONMENT.md).

## Intelligent roles actually used

| Role | Private authority and tools | Output type |
|---|---|---|
| Allbuddy data/product principal | Allbuddy-only source inspection, local probes, refusal of World authority | REQUEST / PROPOSE / COUNTER / REFUSE / UNKNOWN / LOCAL_EXPERIMENT |
| Agent World provenance/projection principal | World-only source inspection, local probes, refusal of business authority | same |
| static baseline agent | pre-state and oracle only; no implementation action | one-shot design |
| least-privilege central evaluator | frozen public interface only | execution feasibility judgment |
| independent open reviewers | source- or World-scoped read/probe authority without a seeded defect list | PASS / COUNTER / BLOCK with reproducer |
| root integrator | local reversible execution and evidence relay | implementation, verification and evidence assembly |

The episode principals were real language-model agents. Deterministic
TypeScript/JavaScript/Python implemented stores, tools, replay and verification;
it did not replace principal choices with prewritten role scripts.

## Model identity limit

The session exposed the Codex GPT-5 model family but did not persist an exact
provider checkpoint, sampling configuration, token count or per-agent model
envelope into the research filesystem. No model override was recorded for the
principal roles. Consequently:

- exact checkpoint comparison is `UNKNOWN`;
- strong-model contribution remains a confound;
- token and monetary accounting are not fabricated.
