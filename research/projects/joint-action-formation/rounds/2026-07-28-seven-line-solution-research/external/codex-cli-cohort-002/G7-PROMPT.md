你是第二批 G7 独立 Codex CLI 主研究会话。工作区：
`/Users/nature/通爻协议研究`。

先逐字读取根 AGENTS.md、research/NOW.md、本轮 PROGRAM.md、G7 Pro return、G7-AUDIT.md、
第一批 CLI G7-final.md、WAVE-010-G6-G7-AUDIT.md、X2 候选合同、当前 G7 line contract。

必须实际使用本 CLI 的多 Agent 能力并行分出三名内部研究者：
A 负责原生 G7 的 reuse/context/legitimacy/reopen/migration 分面；
B 实现真实 runner 与 migration/reconciliation；
C 攻击五态压缩、truth-to-API copy、hidden edge、method alias、migration oracle。
若不能创建子 Agent，明确记录，不得模拟。

只在
`research/projects/joint-action-formation/rounds/2026-07-28-seven-line-solution-research/experiments/wave-011-g7-orthogonal-replay/`
交付 12–20 world 的 `T6-G7-ORTHOGONAL-REPLAY-001` 候选。必须：
1. 把 CURRENT/REVOKED/UNKNOWN/REFUSED/STALE 降为 Authority observation，而非完整控制态；
2. 正交保存 normative stance、epistemic state、channel outcome、freshness/provenance、
   fork/equivocation、Effect phase、coordinator epoch、migration phase、Acceptance applicability；
3. owner services、source runtime、target runtime、effectors、Acceptance owner 与 grader 分域；
4. public side 只见 native response，不免费得到 dependency identity/hidden edge/truth label；
5. actual runner 执行 query、intent、dispatch/fence、response loss、readback、closure、
   local/global reopen、capsule export/import、old-runtime fencing 和 reconciliation；
6. 至少覆盖 no-dispatch-vs-uncertain-effect、stale/fork/refusal、wrong-object Acceptance、
   hidden edge、low/high coupling、planned drain、crash takeover、split-brain、field drop 和
   low-drift control；
7. 比较 B0/B1/mature composite/equal-authority center/delegated center/human-rule baseline，
   不共享 closure function；
8. 逐项返回 T6 R1–R8、unsafe continuation、历史改写、未对账 Effect/obligation、成本。

交付 fixture、private oracle、native provider simulators、独立 workers、runner、tests、README。
先验证 12–20 个高区分 world，不扩到 1500。不得改 NOW/PROGRAM/正式状态。结束时写本 cohort
`G7-final.md`，如实报告实际子 Agent、测试与边界。

