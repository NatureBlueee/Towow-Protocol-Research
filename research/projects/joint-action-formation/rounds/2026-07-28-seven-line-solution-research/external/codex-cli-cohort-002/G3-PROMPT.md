你是第二批 G3 独立 Codex CLI 主研究会话。工作区：
`/Users/nature/通爻协议研究`。

先逐字读取根 AGENTS.md、research/NOW.md、本轮 PROGRAM.md、G3 Pro return、G3-AUDIT.md、
第一批 CLI G3-final.md、X1 outcome-contract v1/validator/tests 与当前 G3 line contract。

必须实际使用本 CLI 的多 Agent 能力并行分出三名内部研究者：
A 负责量词与 Episode/closure 语义；
B 负责最小 simulator/evaluator 实现；
C 专门攻击 post-treatment oracle、inventory completeness、actual-policy miss 与
bounded-unreachable 的误判。
若 CLI 不能创建子 Agent，真实记录，不能模拟。

目标不是证明新 planner 必要。planning/workflow/IAM/强中心/人工制度或其组合完整解决是
正向结果。

只在
`research/projects/joint-action-formation/rounds/2026-07-28-seven-line-solution-research/experiments/wave-011-g3-quantifier-discriminator/`
实现 6-world 候选鉴别器，至少区分：
1. direct qualified path 已存在；
2. complete/frozen inventory 下旧 closure UNSAT，合法新 operator 后 SAT；
3. C=SAT、N=NEW_TOKEN、E=SAME（token 新形成但旧 policy 已含 request/sign）；
4. measurable path 存在但 actual policy miss；
5. open/incomplete inventory，只能 UNKNOWN，不能 bounded-unreachable；
6. material Q/V0/Principal/Authority 改变，必须 authorized new episode 或 invalid substitution。

必须绑定并返回 C/N/E/T/V、六个 R 坐标、inventory completeness、actual policy transcript、
counterfactual、exact task diff。formation witness 不能读取 scorer truth；causal removal
必须在冻结 trace 上实际运行，不能用 expected label。比较 equal-envelope center、
legitimately-centralized environment、mature planner/workflow 与 human rule baseline。

交付 fixture、private oracle、至少两个独立 worker、runner、tests、README。先验证量词
分辨力，不扩到三域 216 episodes。不得改 NOW/PROGRAM/正式状态。结束时写本 cohort
`G3-final.md`，如实报告实际子 Agent、测试和结论。

