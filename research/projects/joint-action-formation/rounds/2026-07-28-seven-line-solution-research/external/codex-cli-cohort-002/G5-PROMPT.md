你是第二批 G5 独立 Codex CLI 主研究会话。工作区：
`/Users/nature/通爻协议研究`。

先逐字读取根 AGENTS.md、research/NOW.md、本轮 PROGRAM.md、G5 Pro return、G5-AUDIT.md、
第一批 CLI G5-final.md、WAVE-009-G2-G5 设计/结果、X1/M01 候选门与当前 G5 line contract。

必须实际使用本 CLI 的多 Agent 能力并行分出三名内部研究者：
A 负责 owner/Authority/四值/native semantics；
B 实现最小 race/fence/conformance simulator；
C 攻击免费 registry、跨 Authority 假原子、迁移语义损失和强中心不公平。
若不能创建子 Agent，明确记录，不得模拟。

成熟 IAM/policy/CLM/reservation/human/强中心完整解决均为正结果。

只在
`research/projects/joint-action-formation/rounds/2026-07-28-seven-line-solution-research/experiments/wave-011-g5-authority-conformance/`
交付 MCB-G5-v2 的小型 discriminator，不要先宣布 canonical IR。至少实现：
1. E1 Unified Authority / Same-Permission crossed pair：真实统一 Authority 与完全相同技术权限
   但存在 non-delegable external right；
2. native outcome preservation：不得预读 ALLOW/REJECT/UNKNOWN/DEFER；用 provider-native
   outcome/error/version/freshness 经 task mapping 派生；
3. 四个 owner 独立 store/key/process，能 reject/revoke/outage/fork；
4. 每个 read/re-read/sign/reserve/execute 边界后注入 race；
5. target-side monotonic fence，含 ignore/restart-loss/cross-region reorder；
6. exact object 扩为 canonicalization、sidecars、external dependencies、materiality rule；
7. Standing lifecycle 与未预登记 stakeholder/late challenge；
8. migration 只声明 witnessed equivalence，保留 native Unknown/forbid/provenance。

若实际 OPA/Cedar/OpenFGA/XACML 全部接入成本过高，先实现 adapter conformance corpus 和至少
一个真实本地可运行引擎；其余明确 NOT_RUN，禁止伪造产品比较。比较 strong center 三类
stratum、mature composition、CLM/HITL、人类规则。交付 fixtures、owner services、
workers、runner、tests、README。不得改 NOW/PROGRAM/正式状态。结束时写本 cohort
`G5-final.md`。

