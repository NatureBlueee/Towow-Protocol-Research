你是第二批 G1 独立 Codex CLI 主研究会话。工作区：
`/Users/nature/通爻协议研究`。

先逐字读取根 AGENTS.md、research/NOW.md、本轮 PROGRAM.md、G1 Pro return、G1-AUDIT.md、
第一批 CLI G1-final.md、当前 G1 line contract 与 WAVE-009-G1-DESIGN。不要只读摘要。

必须实际使用本 CLI 的多 Agent 能力并行分出三名内部研究者：
A 从原问题和 V2 边界独立重建 evaluator；
B 设计/实现最小可运行 discriminator；
C 不看期待结果，攻击 oracle、标签、分母、公平性和 authority/disclosure 泄漏。
若该 CLI 事实上不能创建子 Agent，明确记录 capability failure，不得用三个小标题冒充。

目标不是证明 NAC/通爻独特。ARD/RAG/强中心/模型/人工/成熟组合完整解决均为正结果。

在独立目录
`research/projects/joint-action-formation/rounds/2026-07-28-seven-line-solution-research/experiments/wave-011-g1-provenance-discriminator/`
交付最小候选：
1. 冻结 IntentAtCoordinationInterface，明确排除 vague-goal→Intent；
2. positive label 前独立 INVALIDITY_GATE；
3. 用事件向量取代 INDEX_HIT/MODEL_HIT/ACTIVE_REVELATION 等互斥单标签；
4. 只允许 t0 合法 evidence path，禁止注入 t1 receipt/最终证据；
5. 至少含 public baseline、t0 legal evidence path、final-proposal-only、
   operator removal/reversal、full trace；
6. 区分 L_benchmark 与 D_actual；拒绝/不可区分不算 actual-policy 漏检；
7. 拆分 C-RAW-UPPER 与 C-EQUAL-ACCESS，人类使用同 action envelope 并计成本；
8. 先做 8–12 个高区分 world，自测 truth transplant、post-treatment evidence、
   wrong Authority、forbidden disclosure、同源 alias。

优先交付可执行 fixture、独立 oracle、至少两个真正不同的 baseline worker、runner、
tests 和 README。允许得出 evaluator 仍不能无泄漏运行。不要扩成 30+ episode，不要改
NOW/PROGRAM/正式状态。只在自己的目录编辑。结束时把 final 写入本 cohort 的
`G1-final.md`，报告实际子 Agent、文件、测试、结果与未解决边界。

