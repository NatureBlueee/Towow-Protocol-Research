你是第二批 G4 独立 Codex CLI 主研究会话。工作区：
`/Users/nature/通爻协议研究`。

先逐字读取根 AGENTS.md、research/NOW.md、本轮 PROGRAM.md、G4 Pro return、G4-AUDIT.md、
第一批 CLI G4-final.md、本地 G4 v2 audit/simulator 与当前 G4 line contract。

必须实际使用本 CLI 的多 Agent 能力并行分出三名内部研究者：
A 重建原始“首次成功兑现”与“有界权威终态”双结果；
B 实现最小 paired-world runner；
C 攻击免费 current read/Authority/fence/readback、method alias、强中心不公平和规模幻觉。
若不能创建子 Agent，明确记录，不得模拟。

只在
`research/projects/joint-action-formation/rounds/2026-07-28-seven-line-solution-research/experiments/wave-011-g4-dual-outcome-discriminator/`
交付 12–20 world 候选，要求：
1. 分开 Y_success、Y_resolution、Y_effect、Y_acceptance；
2. 分开 P0 prospective prediction、实际形成动作 I、P1 新冻结预测；
3. passive pair、active-query/commitment pair、full-lawful-interaction-equivalent hard pair
   分开评分；
4. primitive API 返回 raw owner/provider response，不给 head_current/fenced/authoritative 等
   预裁决布尔值；
5. strong center 与 mature composite 使用不同实现；另设 legitimately delegated center；
6. 实际执行 recovery/readback，不接受 label-match；
7. 含 declared-unqueryable、stale-head 两语义、response lost、wrong object、revocation after
   check、reservation≠consent、same-source alias；
8. 同时报 false reliance、safe recall、success confusion matrix、resolution confusion matrix、
   abstention、查询/披露/延迟成本。

交付 fixture、private oracle、primitive services、至少两个独立 worker、runner、tests、README。
先证明 evaluator 与交互量词有分辨力，不扩至 2160/17280。不得改 NOW/PROGRAM/正式状态。
结束时写本 cohort `G4-final.md`，报告实际子 Agent、测试、结果和残余。

