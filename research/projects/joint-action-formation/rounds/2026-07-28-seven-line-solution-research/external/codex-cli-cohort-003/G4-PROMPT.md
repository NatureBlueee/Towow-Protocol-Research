# G4 任务

按 `COMMON.md` 执行。只写：

`experiments/wave-012-ce001-power-restoration/g4-reliance/`

为 CE-001 构建 G4 reliance/outcome module：

- `P0 → interaction → P1 → attempt → Y_success/Y_resolution/Y_effect/Y_acceptance`；
- E3A/E3B ACK-loss pair 与 wrong-object readback；
- passive/active/full-interaction pair 量词；
- commit-time evidence、reservation、readback、reconciliation；
- 将预测校准与 arm 改变世界的能力分开评分；
- blind holdout interface 和 double-submit/revoke 压力。

最终写 `external/codex-cli-cohort-003/G4-final.md`。
