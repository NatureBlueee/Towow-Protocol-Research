# G7 任务

按 `COMMON.md` 执行。只写：

`experiments/wave-012-ce001-power-restoration/g7-evolution/`

为 CE-001 构建 G7 reuse/reopen/migration module：

- E4 撤销后 alternative recovery-to-value；
- E6 Effect 后/Acceptance 前 crash、target takeover、old runtime restart；
- local/global causal reopen、history append-only、Effect reconciliation；
- cold-vs-repeat 对照与最小充分 Context；
- migration capsule field loss 和第二 adapter interface；
- 若构造 hidden pair，valid 分支必须显式要求继续或计 liveness loss，revoked 分支禁止继续；
- 不重复 w010/w011 两侧允许同一保守动作却宣称不可兼得的错误。

最终写 `external/codex-cli-cohort-003/G7-final.md`。
