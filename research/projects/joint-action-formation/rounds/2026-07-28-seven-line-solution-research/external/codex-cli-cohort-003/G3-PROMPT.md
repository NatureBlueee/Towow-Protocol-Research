# G3 任务

按 `COMMON.md` 执行。只写：

`experiments/wave-012-ce001-power-restoration/g3-formation/`

为 CE-001 构建 G3 formation/reachability module：

- 区分 direct path、old full-policy closure、new token、model/kernel change、task change；
- 返回 `C/N/E/T/V` 与 physical/measurable/actual/robust 结果；
- E2 purpose token/delegation 形成，remove/reverse 后从 exact S0 重放；
- E4 撤销后 alternative 的 recovery-to-value；
- open inventory 必须 Unknown；
- 不给五臂共同调用 `choose(packet)`，也不把 operator proposal 预解在 public packet。

最终写 `external/codex-cli-cohort-003/G3-final.md`。
