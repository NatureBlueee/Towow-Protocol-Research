# 研究线 03-B：类型化 Unknown 路由

Contract：`LINE-03-TYPED-UNKNOWN-V2 / v1`

状态：`ACTIVE`。本线只研究 Unknown 类型到 ask、search、probe、tool、partner、
authority、rewrite、Refuse、Defer 或 exit 的可执行选择，以及它相对普通 planner、
tool router、稳定 Schema 强模型和 strong-center + HITL 的净增量。

本线采用 `COMPOSE`：不创建新的事实根，也不预设 PFE 是所有 Intent 必经的顶层引擎。
Planning、action-model learning、主动信息获取、工具调用、审批和人类例外都是可复用组成。
分类分数不是 formation、授权或现实价值证据；Refuse、Defer 与 Protective Contraction
可以是正确结果。

涉及真人偏好、真实 Authority、不可逆 probe、外部写入、Effect、Adoption、Acceptance
或商业净值的子主张均为 `BLOCKED`。`CAP-FORM-005` 的长期编译/重开净值不在本线中，
保持不受影响，而非随 router 结果自动晋升或 `DEFERRED`。
